overlay: 1.1.0
info:
  title: "Reconcile ote-godaddy.com abuse 2.0.0 spec against live API responses"
  version: "1.0.0"
actions:
  - target: "$.servers"
    description: "Add GoDaddy OTE base URL so Jentic can route requests. The servers array was present but empty, causing INGEST-007 quality failure and blocking all Jentic execution."
    update:
      - url: "https://api.ote-godaddy.com"
